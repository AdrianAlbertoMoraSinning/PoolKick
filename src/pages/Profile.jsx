import React,{useState} from 'react'
import { useApp } from '../context/AppContext'
import { avatars } from '../data/seed'
import { Avatar, Button, Card } from '../components/UI'

export default function Profile(){const {currentUser,updateProfile}=useApp();const [f,setF]=useState({...currentUser});const save=e=>{e.preventDefault();updateProfile({displayName:f.displayName,avatar:f.avatar,country:f.country,countryFlag:f.countryFlag,favoriteTeam:f.favoriteTeam});alert('Profile saved.')}
return <><div className="page-head"><div><span className="eyebrow">Player identity</span><h1>Your profile.</h1><p>Choose how friends see you inside pools and leaderboards.</p></div></div><div className="profile-grid"><Card className="profile-preview"><Avatar user={f} size="xl"/><h2>{f.displayName}</h2><p>{f.countryFlag} {f.favoriteTeam||'Football fan'}</p><small>{currentUser.email}</small></Card><Card className="panel"><form onSubmit={save}><label>Display name<input value={f.displayName} onChange={e=>setF({...f,displayName:e.target.value})}/></label><div className="form-two"><label>Country code<input placeholder="CO" value={f.country||''} onChange={e=>setF({...f,country:e.target.value.toUpperCase()})}/></label><label>Country flag<input placeholder="🇨🇴" value={f.countryFlag||''} onChange={e=>setF({...f,countryFlag:e.target.value})}/></label></div><label>Favorite team<input placeholder="Colombia" value={f.favoriteTeam||''} onChange={e=>setF({...f,favoriteTeam:e.target.value})}/></label><label>Choose an avatar</label><div className="avatar-picker">{avatars.map(a=><button type="button" className={f.avatar===a?'selected':''} key={a} onClick={()=>setF({...f,avatar:a})}>{a}</button>)}</div><Button type="submit">Save profile</Button></form></Card></div></>}
